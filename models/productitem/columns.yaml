# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
    title:
        label: Title
    slug:
        label: Slug
    created_at:
        label: Date Created
        type: date
        invisible: true
    updated_at:
        label: Last Update
        type: date